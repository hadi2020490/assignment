  let numbers = [10, 20, 30, 40, 50];
  let total = 0;

  for (let i = 0; i < numbers.length; i++) {
    total += numbers[i];
  }

  alert("Sum: " + total);

  let number = [1, 2, 3, 4, 5];
  for (let i = 0; i < numbers.length; i++) {
    alert(numbers[i]);
  }

for (let i = 1; i <= 3; i++) {
    for (let j = 1; j <= 3; j++) {
      alert("i: " + i + ", j: " + j);
    }
  }

  let name = "Ali Khan";
  alert(name.toUpperCase());
  alert(name.toLowerCase());

let text = "JavaScript is good";
  alert(text.length);
  alert(text.slice(0, 10));
  alert(text.substring(11, 13));
  alert(text.charAt(4));

